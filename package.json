{
  "name": "ecopulse-render-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install --production",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}